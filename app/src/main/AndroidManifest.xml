<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.diegomalone.a11y">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CounterActivity"
            android:label="@string/counter_activity_title" />
        <activity
            android:name=".ListActivity"
            android:label="@string/list_activity_title" />
        <activity
            android:name=".FocusOrderActivity"
            android:label="@string/focus_order_activity_title" />
        <activity
            android:name=".NoToolbarActivity"
            android:theme="@style/NoToolbarTheme" />
        <activity
            android:name=".AndroidWidgetsActivity"
            android:label="@string/android_widgets_activity_title" />
    </application>

</manifest>